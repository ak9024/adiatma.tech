definitions:
  hadith.Hadith:
    properties:
      arab:
        type: string
      id:
        type: string
      number:
        type: integer
    type: object
info:
  contact:
    email: adiatma.mail@gmail.com
  title: Adiatma Tech API
  version: v0.1.0
paths:
  /hadith/{author}:
    get:
      parameters:
      - description: author
        in: path
        name: author
        required: true
        type: string
      - description: page
        in: query
        name: page
        type: string
      - description: perPage
        in: query
        name: perPage
        type: string
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/hadith.Hadith'
            type: array
swagger: "2.0"
